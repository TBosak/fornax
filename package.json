{
  "name": "fornax",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "imports": "bun run scripts/generate-imports.ts",
    "main": "bun build main.ts --outdir dist",
    "start": "bun run imports && bun run main && bun run index.ts"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/bun": "latest",
    "@types/parse5": "^7.0.0",
    "@vaadin/router": "^2.0.0",
    "incremental-dom": "^0.7.0",
    "parse5": "^7.2.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  }
}